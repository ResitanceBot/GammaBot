<launch>

  <!-- RVIZ -->
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find map_marker)/rviz/map_marker.rviz" />

  <!-- map_marker node -->
  <node pkg="map_marker" type="map_marker" name="map_marker" output="screen"/>

</launch>